/**
 * The maker package contains classes to make worlds from definitions contained in XML files. The
 * files are able to define the configuration for the world as well as the elements that are placed
 * within it. The format of the XML files are described in {@link Loader}.
 */
package neurevolve.maker;
